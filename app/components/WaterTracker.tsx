import React from 'react';
import { View, Text } from 'react-native';

// Stub component - original was Convex-based
// Use SimpleWaterTracker instead for PostgreSQL functionality
const WaterTracker = () => {
    return (
        <View>
            <Text>Water Tracker - Use SimpleWaterTracker instead</Text>
        </View>
    );
};

export default WaterTracker;
